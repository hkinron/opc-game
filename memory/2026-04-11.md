# 2026-04-11

## Pixel Agents — 迭代开发

### 迭代 #1 (06:05 UTC) — 工位个人物品 + 打工人对话增强

**方向A: 工位个人物品**
- 新增 3 个 TileType: DeskCup(水杯), DeskPlant(小盆栽), DeskPhoto(相框)
- 每种物品都有独立的像素渲染:
  - DeskCup: 随机杯色(7种), 偶尔冒热气动画
  - DeskPlant: 花盆 + 摇晃的绿叶 + 新芽
  - DeskPhoto: 木质相框 + 风景照片(蓝天绿地小太阳)
- 6 个工位各有不同的物品组合，让每个工位有个性
- 构建: 21 modules → 83.81KB (gzip 26.25KB)

**方向B: 打工人对话增强**
- CHAT_PAIRS 从 18 条扩展到 30 条
- 新增 12 条共鸣对话: "需求又改了""帮我带杯咖啡""发薪日""晨会好长""代码一次就跑通了" 等
- 覆盖了外卖、空调、加班、辞职开咖啡店等经典打工人场景

**改动文件:**
- `src/types/index.ts` — 新增 3 个 TileType
- `src/engine/TileMap.ts` — 配置 + FURNITURE_MAP
- `src/engine/Renderer.ts` — drawDeskCup / drawDeskPlant / drawDeskPhoto
- `src/engine/ConfigSystem.ts` — 布局中添加工位个人物品
- `src/engine/InteractionSystem.ts` — 扩展对话池

### 迭代 #2 (06:35 UTC) — 饮水机 + 午休行为升级

**方向A: 饮水机 (WaterCooler)**
- TileType.WaterCooler 已有类型和渲染(drawWaterCooler)，但布局中缺失
- 在 ConfigSystem.ts 的 DEFAULT_LAYOUT 中添加饮水机到茶水间区域 (9,9)
- 在 InteractionSystem.ts 中添加饮水机交互: 点击派 agent 去"接水喝"
- 渲染特点: 桶装水瓶 + 动态水位 + 接水按钮 + 纸杯架

**方向B: 午休行为升级**
- 之前: 午休时间(12:00-13:00 中国时间)只是 skip simulation，agent 原地不动
- 现在: 午休时 15% 概率 agent 离开座位去吃饭/休息
  - 🍱 吃自带午餐 (9,7)
  - 🔥 微波炉热饭 (8,7)
  - 🍪 饭后零食 (10,9)
  - ☕ 饭后咖啡 (10,10)
  - 🛋️ 沙发午休 (1,7)
- 到达后停留 6 秒，然后在附近晃 5-10 秒，最后回工位工作
- 更有真实感: 午休时办公室里一半人不在座位上

**改动文件:**
- `src/engine/ConfigSystem.ts` — 添加 watercooler 到布局
- `src/engine/InteractionSystem.ts` — 添加 watercooler 交互 + 类型扩展
- `src/engine/Game.ts` — 午休行为: agent 离开座位去吃饭/休息

**构建:** 21 modules → 84.66KB (gzip 26.50KB)

### 迭代 #5 (07:35 UTC) — 入口区升级：雨伞架 + 打卡机

**方向A: 入口区真实感升级**
- 新增 2 个 TileType: UmbrellaStand(雨伞架)、AttendanceMachine(打卡机)
- **雨伞架 (UmbrellaStand)**:
  - 位置: 入口左侧 (7,11)，靠近电梯
  - 渲染: 金属桶身 + 4 把不同颜色的折叠雨伞（红/蓝/绿/黄/紫/青随机排列）
  - 动画: 雨伞微微摇晃 + 底部偶尔出现水滴（像刚下雨回来）
  - 交互: 点击派 agent 去"放/拿雨伞"
- **打卡机 (AttendanceMachine)**:
  - 位置: 入口右侧墙边 (13,11)
  - 渲染: 壁挂式机身 + 实时时钟显示 + "打卡"闪烁文字
  - 刷卡感应区有动态波纹动画（WiFi 波纹效果）
  - 绿色 LED 指示灯 + 指纹识别区域
  - 交互: 点击派 agent 去"滴！打卡成功！"

**Bug修复: 饮水机坐标修正**
- InteractionSystem.ts 中 watercooler 坐标从 (9,9) 修正为 (15,10)，与 ConfigSystem 布局一致
- nearbyTile 从 (8,9) 修正为 (14,10)

**改动文件:**
- `src/types/index.ts` — 新增 2 个 TileType
- `src/engine/TileMap.ts` — 配置 + FURNITURE_MAP
- `src/engine/Renderer.ts` — drawUmbrellaStand / drawAttendanceMachine
- `src/engine/ConfigSystem.ts` — 布局中添加雨伞架 + 打卡机
- `src/engine/InteractionSystem.ts` — 新增 2 个交互对象 + 修复饮水机坐标

**构建:** 21 modules → 89.92KB (gzip: 27.78KB)
**注意:** GitHub Pages 推送失败（cron session 无 GITHUB_TOKEN），源码已 push 到 main

### 迭代 #4 (07:05 UTC) — 导向标识升级 + 每日站会

**方向A: 导向标识升级为真实多方向路牌**
- `src/engine/Renderer.ts` — drawSignpost 重写
- 从"轮播单方向文字"改为"三层彩色箭头路牌"
- 三个方向同时显示：→会议室(蓝)、←茶水间(绿)、↓电梯口(黄)
- 每个方向有独立箭头形状（右指/左指/直行），像真实办公室指示牌
- 增加了底座和标识杆细节

**方向B: 每日站会 (Morning Standup)**
- `src/engine/Game.ts` — 新增 standupTriggered 标志和晨会逻辑
- 工作日 9:00-10:00 (中国时间) 触发晨会
- 所有 agents 聚集到白板区域 (2-4, 2-3)，每人报自己的工作内容
- 站会对话：「今天做登录模块」「修了3个bug」「部署监控系统」等
- 10-15 秒后自动散会，agents 各自回工位开始工作
- 触发时显示事件横幅：「📋 每日站会！大家到白板前集合！」

**改动文件:**
- `src/engine/Renderer.ts` — drawSignpost 完全重写
- `src/engine/Game.ts` — 晨会逻辑 + standupTriggered 状态管理

**构建:** 21 modules → 85.97KB (gzip 26.91KB)

### 迭代 #6 (08:05 UTC) — 团队区域分隔 + 茶水间闲聊系统

**方向A: 团队区域分隔带**
- `src/engine/ConfigSystem.ts` — 在上下两排工位之间 (y=7) 增加绿植分隔带
  - 前端团队区: (5,7), (6,7)
  - 后端团队区: (12,7), (13,7)
  - 中间保留走廊通道 (x=9,10 地毯走廊)
  - 视觉效果: 像真实办公室里用绿植划分不同团队的办公区

**方向B: 茶水间闲聊系统 (Tea Room Gossip)**
- `src/engine/Game.ts` — 新增 `checkTeaRoomGossip()` 方法和 `TEA_ROOM_GOSSIP` 对话池
  - 检测 2 个 agent 同时出现在茶水间区域 (15-18, 8-10) 时触发专属 gossip
  - 45 秒冷却时间 (同 pair)
  - 对话内容: "中午吃啥""加班到几点""隔壁组裁员""想转行独立开发"等 15 条
  - 专属场景感: 只有在茶水间才会聊这些摸鱼话题
  - agent 面对面站立对话
- 构建: 21 modules → 91.80KB (gzip 28.63KB)

**改动文件:**
- `src/engine/ConfigSystem.ts` — 添加团队区域分隔绿植 (4个)
- `src/engine/Game.ts` — 新增 gossipCooldown、checkTeaRoomGossip()、TEA_ROOM_GOSSIP

### 之前的部署信息

## Pixel Agents — 部署与迭代

### GitHub Pages 部署
- 仓库: https://github.com/hkinron/opc-game
- 页面: https://hkinron.github.io/opc-game/
- Token: ${GITHUB_TOKEN}
- 部署流程: 构建到 dist/ → 复制到 /tmp/gh-pages-repo → push 到 gh-pages 分支
- vite.config.ts 必须设置 `base: '/opc-game/'` 才能正确加载资源
- 当前构建 hash: index-DSCneQ-6.js (89.92KB, gzip 27.78KB)

### 代码仓库
- 源码: /home/node/.openclaw/workspace/hjr-agent/pixel-agents/
- Git remote: https://${GITHUB_TOKEN}@github.com/hkinron/opc-game.git (main 分支)
- 构建命令: cd pixel-agents && pnpm run build
- 部署脚本模板:
  ```
  cd /tmp/gh-pages-repo && rm -rf * .git && git init && git checkout -b gh-pages
  cp -r pixel-agents/dist/* .
  git config user.email "hjr@pixelagents.dev" && git config user.name "HJR"
  git add -A && git commit -m "..."
  git remote add origin https://${GITHUB_TOKEN}@github.com/hkinron/opc-game.git
  git push -f origin gh-pages
  ```

### Agent 行为调优历史
- 初始: agent 不走动，全部原地 idle/typing
- 第一次调整: 25% 概率每 2 秒走动，太快
- 第二次调整: 10% 每 6 秒，还是快
- 第三次调整: 5% 每 10 秒 + 0.5s/格
- 最终: 3% 每 12 秒 + 0.8s/格 (Agent.ts moveTimer > 0.8, Game.ts simTimer > 12, rand < 0.03)
- 散步目的地: 咖啡机(7,4)、沙发(1,6)、白板(2,3)、书架(10,3)、串门(6,8)
- 到达后停留 8 秒再走回工位

### 技术要点
- pnpm 安装依赖 (npm 在 workspace 下有 bug)
- Sealos 容器只有 3000 端口可访问，其他端口被 Ingress 挡住
- Canvas/Browser 工具在容器里不可用
- OSS data URI 链接有时效性
- Vite 构建后 JS 文件名每次不同 (hash)，部署时要用 dist/ 里的实际文件
